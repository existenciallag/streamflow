[server]
headless = true
port = 8501
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false
# Do not try to open a browser automatically — launcher.py does that
serverAddress = "localhost"

[theme]
base = "dark"

[logger]
level = "error"
